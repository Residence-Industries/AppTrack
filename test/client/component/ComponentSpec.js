import React from 'react';
import { expect } from 'chai';
import { shallow, mount, render } from 'enzyme';

describe('Component Test', function () {
  it('should pass', function () {
    const testExample = true;
    expect(testExample).to.equal(true);
  });
});
